query RunInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          product {
            volumeTiers: metafield(namespace: "custom", key: "volume_tiers") {
              jsonValue
            }
          }
        }
      }
    }
  }
}
